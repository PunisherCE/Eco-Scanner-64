using UnityEngine;

// Cursor management removed: the system default cursor is now always used
// and never hidden or locked, so UI interaction is never blocked.
public class CursorManager : MonoBehaviour
{
}
